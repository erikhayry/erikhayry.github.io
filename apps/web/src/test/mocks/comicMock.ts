import type {Comic} from "@library/types";
import {PAGE_1_MOCK} from "./pageMock";

export const COMIC_SLUG_MOCK_1 = "comic-mock-1"

export const COMIC_MOCK_1: Comic = {
    "slug": COMIC_SLUG_MOCK_1,
    "pages": [
        PAGE_1_MOCK
    ]
}