{
  "name": "the-jutas-project",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test:bundler": "cd apps/bundler && npm run test",
    "test:web": "cd apps/web && npm run test",
    "test": "npm run test:bundler && npm run test:web",
    "bundle": "npm run index.ts",
    "prepare": "husky"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "library/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "@library/types": "workspace:*",
    "typescript": "^5"
  },
  "dependencies": {
    "@rollup/rollup-darwin-x64": "^4.52.5",
    "zod": "^4.1.12"
  }
}
